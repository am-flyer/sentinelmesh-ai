# CyberSecurity Multi-Agent System — Dependencies
# Python >= 3.10 required

# ── LangGraph (orchestration state machine) ────────────────────────────
langgraph>=0.2.0
langchain>=0.3.0
langchain-community>=0.3.0
langchain-openai>=0.2.0          # OpenAI-compatible client (used for OpenRouter)

# ── LLM: OpenRouter (OpenAI-compatible API, 200+ models) ──────────────
openai>=1.40.0                   # OpenRouter uses OpenAI SDK format

# ── Embeddings: HuggingFace (runs locally, no API key) ────────────────
sentence-transformers>=3.0.0     # Local embedding models
huggingface-hub>=0.24.0
torch>=2.0.0                     # CPU-only fine for embeddings

# ── Vector Store: ChromaDB (persistent local DB) ──────────────────────
chromadb>=0.5.0

# ── RAG Framework: LlamaIndex ─────────────────────────────────────────
llama-index>=0.11.0
llama-index-core>=0.11.0
llama-index-vector-stores-chroma>=0.2.0
llama-index-embeddings-huggingface>=0.3.0

# ── UI: Gradio ─────────────────────────────────────────────────────────
gradio>=4.0.0                    # Web UI for the multi-agent system

# ── Observability: LangSmith ─────────────────────────────────────────────
langsmith>=0.1.0                     # LangChain tracing & observability

# ── Graph Visualization ────────────────────────────────────────────────
matplotlib>=3.7.0                # Fallback graph rendering
grandalf>=0.8                    # LangGraph draw_png/draw_mermaid support

# ── Utilities ──────────────────────────────────────────────────────────
python-dotenv>=1.0.0             # Load .env API keys
rich>=13.0.0                     # Pretty terminal output (optional)
